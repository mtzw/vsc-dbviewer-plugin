# v3.4.2 受入確認記録

確認日: 2026-07-23

## 自動確認

| 項目 | 結果 | 確認内容 |
| --- | --- | --- |
| TypeScript回帰テスト | 成功 | 91テスト。差分分類、LOB除外、出力、チャンク取得、上限、キャンセル、ページング、フィルター、互換読込、破損検出を含む |
| Java方言テスト | 成功 | JDK 25から`--release 17`でコンパイルして実行 |
| H2 JDBC統合テスト | 成功 | H2 2.4.240で接続、メタデータ、取得、検索、ソート、ページング、Insert / Update / Delete / rollbackを確認 |
| TypeScriptコンパイル | 成功 | `tsc -p .` |
| クリーンビルド | 成功 | `npm run build`。Java helperを`--release 17`で生成 |
| VSIX生成・内容検査 | 成功 | `vsc-dbviewer-plugin-3.4.2.vsix`。32ファイル、テスト・source map・Javaテストclassの混入なし。SHA-256: `d261c0ebe39b372ebc7c078e4d8ffa7c41e07912cf45f98218c5c54d539481ac` |

## v3.4.2で追加した受入ケース

- 未変更のサマリー比較経路でも同一サイズのチャンク破損を検出する。
- 欠損チャンクのファイル名と、不正マニフェストの原因を表示する。
- v3.4単一ファイルとチャンク形式を比較・削除できる。
- v3.4単一ファイルにも詳細比較の容量上限を適用する。
- LOB / バイナリ列だけの構成変更を検出する。
- LOB / バイナリ型の主キー値を保存せず、不完全な複合主キーによる行分類を行わない。
- ページ取得が進行しない場合に失敗させ、不完全チャンクを削除する。
- 複数パネル相当の同時保存でも索引項目を失わない。

## 実機確認

| 環境 | 状態 | 備考 |
| --- | --- | --- |
| VS Code 1.130.0 | 未実施 | CLIは確認済み。対話可能なExtension Hostへ接続できないため、Webview操作は別途確認する |
| Microsoft SQL Server | 未実施 | JDBCドライバーと接続先がこの環境にない |
| Oracle | 未実施 | JDBCドライバーと接続先がこの環境にない |
| PostgreSQL | 未実施 | JDBCドライバーと接続先がこの環境にない |
| MySQL | 未実施 | JDBCドライバーと接続先がこの環境にない |

実機確認では、スナップショット保存・一覧・比較・削除、進捗・キャンセル、50件ページング、フィルター、行詳細、JSON / CSV保存、機微情報の注意表示を確認し、この表へ結果を追記する。
