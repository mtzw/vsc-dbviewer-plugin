# VSCode DBViewer Plugin実装計画

## 概要
* VSCodeとその互換エディタ向けプラグイン
* Java開発者向けとする

## 基本機能
* 主要なRDBに接続可能とする
* JDBCドライバーを使用して接続する
* まずはTable、Viewをツリー形式で選択可能とする
  - 開くと"Table(View)情報"、"データ"、"定義SQL"をタブ切り替え表示可能とする
* レコードを選択してSQL文としてコピーする
* レコードを選択してTSV形式の文字列としてコピーする

## 実装済みの主要機能
* 接続プロファイルの作成、編集をWebviewのページ形式で提供する
* 接続プロファイルを複製できる
* Table、Viewの詳細画面に「制約」、「インデックス」タブを表示する
* テーブルデータをCSV / TSV / InsertSQL形式で一括エクスポートできる
* TSV形式のレコードをペーストしてInsertできる
* 「データ」タブの機能拡充
  - データリロード
  - ヘッダ項目クリックによるデータソート
  - where句相当の条件を指定する簡易データ検索
  - 選択行削除（主キーで一意に特定できるTable対象）
  - 行Update（コミット、ロールバック対応）

## 推奨ロードマップ
* v2.1
  - [実装済み] 接続プロファイルの作成、編集UIをWebviewのページ形式に変更する
  - [実装済み] 接続プロファイルの複製機能を追加する
  - [実装済み] 接続テスト結果やJDBCドライバー/サポートJAR選択を分かりやすく表示する
* v2.1.x
  - [実装済み] UX改善としてツリー情報のリフレッシュ導線を強化する
    - [実装済み] ビュー右上からRefreshできるようにする
    - [実装済み] 接続プロファイルの追加、編集、削除、複製後は自動Refreshする
    - [実装済み] 接続ノード右クリックからRefreshできるようにする
    - [実装済み] Tables/Viewsフォルダ単位でRefreshできるようにする
    - [実装済み] READMEに、DB側でTable/Viewを追加・削除した場合はRefreshする旨を記載する
* v2.2
  - [実装済み] Table、View詳細に「制約」タブを追加する
  - [実装済み] Table、View詳細に「インデックス」タブを追加する
  - [実装済み] 主キー、外部キー、Unique、Indexなどのメタデータ取得を強化する
  - [実装済み] InsertSQLコピーでOracleの日付/時刻型に対応する
    - [実装済み] JDBC型情報を取得し、DATE/TIMESTAMP/TIMESTAMP WITH TIME ZONEなどを判別する
    - [実装済み] Oracle向けにDATEリテラル、TIMESTAMPリテラル、必要に応じてTO_DATE/TO_TIMESTAMP形式を生成する
    - [実装済み] PostgreSQL/MySQL/H2は現状の標準寄りリテラルを維持しつつ、型情報を使った改善を検討する
* v2.3
  - [実装済み] テーブルデータの一括エクスポート機能を追加する
  - [実装済み] CSV/TSV/InsertSQL形式での保存に対応する
  - [実装済み] 大量データ向けにページング取得、進捗表示、キャンセルに対応する
* v2.4
  - [実装済み] 「データ」タブの読み取り系機能を拡充する
  - [実装済み] データパネルのレイアウトを調整する
    - [実装済み] チェックした行を視覚的にわかりやすくする
    - [実装済み] テーブル上部の操作UIをボタンとして分かりやすく整理する
    - [実装済み] コピー系と保存系の操作をグルーピングする
    - [実装済み] 動作がわかりやすくなるようにアイコンなどの使用を検討する
  - [実装済み] データリロード機能を追加する
  - [実装済み] ヘッダ項目クリックによるデータソートに対応する
  - [実装済み] 簡易データ検索機能を追加する
    - [実装済み] where句相当の条件指定を想定
* v3.0
  - [実装済み] TSV形式のレコードをペーストしてInsertできるようにする
    - [実装済み] 対象はデータタブのTableのみとし、Viewや任意SQL実行は対象外にする
    - [実装済み] TSVはテーブル列順で解釈し、列数不一致はInsert前にエラー表示する
    - [実装済み] 空欄は空文字、`\N`はNULLとして扱う
    - [実装済み] Insert前に対象テーブル、行数、列、先頭行、NULL件数をプレビューする
  - [実装済み] 書き込み前の確認、トランザクション、失敗時の扱いを設計する
    - [実装済み] 確認後にPreparedStatementでInsertする
    - [実装済み] 全行を1トランザクションで実行し、失敗時はrollbackする
    - [実装済み] 成功後は現在の検索、ソート条件を維持してデータを再読み込みする
* v3.1
  - [実装済み] 「データ」タブで選択行を削除できるようにする
  - [実装済み] 削除対象は主キーで一意に特定できるTableのみとする
  - [実装済み] 削除前に対象テーブル、選択行数、主キー値、先頭行をプレビューする
  - [実装済み] TSV Insertと同じ書き込み前確認、トランザクション、失敗時rollbackを適用する
  - [実装済み] 成功後は現在の検索、ソート条件を維持してデータを再読み込みする
* v3.2
  - [実装済み] 「データ」タブで行Updateできるようにする
  - [実装済み] 対象は主キーで一意に特定できるTableのみとし、Viewや任意SQL実行は対象外にする
  - [実装済み] 主キー列はv3.2では更新対象外にする
  - [実装済み] 更新前に対象テーブル、更新行数、主キー値、変更列、変更前/変更後の先頭行をプレビューする
  - [実装済み] 確認後にPreparedStatementでUpdateする
  - [実装済み] 全更新を1トランザクションで実行し、途中で失敗した場合は全体をrollbackする
  - [実装済み] 成功後は現在の検索、ソート条件を維持してデータを再読み込みする
  - [実装済み] 同時更新検知はv3.2では主キー一致のみとし、元値一致チェックは今後検討する
* v3.2.1
  - [実装済み] Insert / UpdateのPreview時に、JDBC型情報に基づく入力値チェックを追加する
  - [実装済み] 数値、真偽値、日付、時刻、タイムスタンプ、NULL可否を検証する
  - [実装済み] Update画面ではDATE / TIME / TIMESTAMP列に日付・時刻入力UIを使用する
    - [実装済み] DATEはカレンダー入力を使用する
    - [実装済み] TIMEは時刻入力を使用する
    - [実装済み] TIMESTAMPは日時入力を使用する
  - [実装済み] DB固有形式やTIMESTAMP WITH TIME ZONEの高度な入力UIは今後検討する
  - [実装済み] 検証ルールはJava helperの型変換で受け付ける標準形式に揃える
* v3.2.2
  - [実装済み] Java helperの配布互換性とデータ取得の安定性を改善する
  - [実装済み] Java helperを`javac --release 17`でビルドし、実行要件をJava 17以上に固定する
  - [実装済み] 主キーまたはUnique Indexを優先して、追加ロードと全件エクスポートの取得順を安定させる
  - [実装済み] Identity、generated、computedなど自動生成列のメタデータを取得する
  - [実装済み] 自動生成列はTSV Insertと行Updateの入力対象から除外し、Java helperでも書き込みを拒否する
  - [実装済み] BINARY / BLOBはBase64、CLOB / SQLXMLは文字列、UUIDは文字列表現へ正規化する
  - [実装済み] Update / Deleteの実行件数が要求件数と一致しない場合に全体をrollbackする
  - [実装済み] 配布VSIXからテストコード、source map、Javaテストclassなどの不要物を除外する
* v3.3
  - [実装済み] Microsoft SQL Serverに正式対応する
  - [実装済み] 接続プロファイルにSQL Serverを追加する
    - [実装済み] JDBCドライバークラスは`com.microsoft.sqlserver.jdbc.SQLServerDriver`を既定値とする
    - [実装済み] `jdbc:sqlserver://host:1433;databaseName=database;encrypt=true`形式の入力例を表示する
    - [実装済み] JDBCドライバーJARは他DBと同様に利用者が指定する
  - [実装済み] DB固有処理を方言単位に整理する
    - [実装済み] SQL Serverでは角括弧による識別子の引用を使用する
    - [実装済み] SQL Serverの`OFFSET ... FETCH`とMySQLの`LIMIT ... OFFSET`を方言として分離する
    - [実装済み] Insert SQLコピー / 保存でbit、Unicode文字列、バイナリのSQL Serverリテラルを生成する
  - [実装済み] SQL Server固有型を確認する
    - [実装済み] `datetime2`、`datetimeoffset`、`money`、`uniqueidentifier`、`bit`の表示・入力経路を追加する
    - [実装済み] `nvarchar(max)`をUnicode文字列、`varbinary(max)`をBase64 / 16進リテラルとして扱う
    - [実装済み] SQL Serverの`timestamp` / `rowversion`を日時型として扱わず、自動生成列として扱う
    - [実機確認済み] 利用者環境のSQL ServerとMicrosoft JDBC Driverで、接続、表示、書き込みを含む一通りのスモークテストを実施する
  - [実装済み] 一時SQL Server 2022コンテナを使用した統合テストを追加する
    - [実装済み] 接続、Table / View一覧、カラム、主キー、外部キー、Indexを確認する
    - [実装済み] データ表示、検索、ソート、ページング、SQL Server固有型を確認する
    - [実装済み] Insert、Update、Delete、rollback、自動生成列の除外を確認する
  - [実装済み] TSV Insertでダブルクォートによるフィールド囲み、`""`による引用符エスケープ、引用フィールド内のタブと改行を扱う
* v3.4
  - [初期実装済み] テーブル単位の差分確認機能のMVPを追加する
  - [初期実装済み] Table詳細の「差分」タブから、変更前のスナップショットを保存できるようにする
  - [初期実装済み] 変更後に同じ対象を再取得し、変更前後を比較する
  - [初期実装済み] Tableごとに件数と行順に依存しない内容フィンガープリントを比較する
    - [初期実装済み] 件数が同じUpdateも検出対象とし、件数だけでは変更有無を判定しない
  - [初期実装済み] 主キーを取得できるTableでは、追加、削除、更新を行単位で分類する
  - [初期実装済み] 主キーを取得できないTableでは、件数と内容変更の検出までとし、行単位差分は保証しない
  - [初期実装済み] サマリーとTable差分をCSV / JSON形式で出力する
  - [初期実装済み] スナップショットはVS Codeの拡張用保存領域に保存し、30日保持と削除操作を提供する
  - [初期実装済み] LOB / バイナリ列を除外し、機微情報が暗号化されず保存されることを画面とREADMEに明示する
  - [初期実装済み] MVPの安全上限を1 Tableあたり100,000行とし、超過時は保存せずエラーにする
  - [確認済み] 差分ロジック、保存・読込・削除・保持期間をTypeScript単体テストで確認する
  - [確認済み] H2を使用した既存のJDBC統合テストと全TypeScriptテストを通過させる
  - [次版へ移行] 上記初期実装の安定化と完成判定はv3.4.2で実施する
* v3.4.1
  - [実装済み] 大規模Table向けに差分取得を改善する
  - [実装済み] 1,000行単位でページ取得結果をチャンク保存し、取得全体をメモリへ保持しない
  - [実装済み] チャンク単位SHA-256と、行順・チャンク境界に依存しない全体フィンガープリントを保存する
  - [実装済み] 進捗へ行数、保存容量、チャンク数を表示し、キャンセル時は不完全データを削除する
  - [実装済み] 1 Tableあたり5,000,000行、512 MiBの容量上限を適用する
  - [実装済み] 異常終了で残った取得中データと比較用一時データを24時間後に削除する
  - [実装済み] 未変更時と主キーなしTableは全行を展開せず、メタデータとフィンガープリントで判定する
  - [実装済み] 行単位詳細は各100,000行、128 MiB以内とし、超過時は理由付きサマリーへ切り替える
  - [実装済み] 行差分を50件単位でページ表示し、全件を順に確認できるようにする
  - [実装済み] Status、主キー、変更列による行差分フィルターを追加する
  - [実装済み] 行詳細ダイアログで列単位の変更前後を省略せず表示し、変更対象列を強調する
  - [検討済み] offsetからの途中再開は更新中の重複・欠落を防げないため実装せず、失敗後は先頭から再取得する
  - [定義済み] 対応DBの取得整合性はページごとに独立した要求を使用する`best-effort`とし、取得中更新時は複数時点が混在し得ることを明示する
  - [自動確認済み] 複数ページ取得、キャンセル、行数・容量上限、取得中の列構成変更、チャンク破損、JSON / CSV出力を確認する
* v3.4.2 差分機能の安定化
  - [実装・自動確認済み] v3.4初期実装の機能受入項目を回帰テスト化する
    - スナップショットの保存、一覧、比較、削除、30日保持をVS Code実機で確認する
    - 追加、削除、更新、変更なし、主キーなし、列・主キー構成変更を含む比較結果を確認する
    - LOB / バイナリ列の除外と、画面上の機微情報に関する注意表示を確認する
    - JSON / CSV出力について、サマリー、行差分、エスケープ、詳細上限理由を確認する
  - [実装・自動確認済み] v3.4.1の大規模データ対応を安定化する
    - 複数チャンクとなるTableで取得進捗、保存容量、比較時間、メモリ使用量を計測する
    - 100,000行以下の行詳細生成と、100,000行または128 MiB超過時のサマリー切替を確認する
    - キャンセル、5,000,000行超過、512 MiB超過、取得失敗後に不完全データが残らないことを確認する
    - 50件ページング、Status・主キー・変更列フィルター、列単位の行詳細を確認する
  - [実装・自動確認済み] 保存形式の互換性と復旧動作を確認する
    - v3.4単一ファイル形式を読み込み、v3.4.1チャンク形式との比較と削除ができることを確認する
    - 破損チャンク、欠損チャンク、不正マニフェストを検出し、利用者に原因を表示できることを確認する
    - 異常終了で残った取得中データと比較用一時データが24時間後に削除されることを確認する
  - [一部確認済み] 対応DBで差分機能の回帰確認を行う
    - H2の自動統合テストを継続し、Microsoft SQL Serverで実機スモークテストを行う
    - Oracle、PostgreSQL、MySQLは利用可能な検証環境から順に、ページング順序と型正規化を重点確認する
  - [完了条件] 全自動テストとビルドが成功し、重大度High以上の既知不具合がなく、上記実機確認結果を記録する
  - [実機確認待ち] VS Code画面操作と、H2以外の利用可能な実DB環境での結果は`ACCEPTANCE-v3.4.2.md`へ追記する

## 現行ロードマップ外の候補
* Java Entity / Record生成
  - Table / Viewからの生成、型マッピング、命名規則、package、JPA / Jakarta対応を想定する
  - v3.4.2の差分機能安定化を優先し、現時点では実装時期を定めない

## 実装上の既知事項
* v3.3時点でOracle、PostgreSQL、MySQLの実DB統合テストは未整備
* 主キーとUnique Indexのどちらも取得できないTable / Viewでは、一意な並び順を決定できないためページングの取得順を保証できない
* 行Updateの同時更新検知は主キー一致のみであり、元値一致またはrowversionによる競合検知は今後検討する
* v3.4形式の既存スナップショットは互換読込時に全行をメモリへ展開する。v3.4.1で新規取得するスナップショットはチャンク形式を使用する
* v3.4.1でもページ間を単一DBトランザクションで固定しないため、取得整合性は全対応DBでbest-effortとなる
* 複数Tableの一括差分比較は、DB負荷、Table間整合性、総保存容量の設計と合わせて今後検討する

## データパネルのレイアウト調整
* [実装済み] チェックした行を視覚的にわかりやすくしたい
* [実装済み] テーブル上部の操作UIが不親切
  - [実装済み] ボタンのようなUIにする
  - [実装済み] コピー系と保存系のボタンをまとめる
* [実装済み] 動作がわかりやすくなるようにアイコンなどを使用した表現も検討する
