# VSCode DBViewer Plugin実装計画

## 概要
* VSCodeとその互換エディタ向けプラグイン
* Java開発者向けとする
  - UI上からEntity生成できるような機能を将来的に追加

## 基本機能
* 主要なRDBに接続可能とする
* JDBCドライバーを使用して接続する
* まずはTable、Viewをツリー形式で選択可能とする
  - 開くと"Table(View)情報"、"データ"、"定義SQL"をタブ切り替え表示可能とする
* レコードを選択してSQL文としてコピーする
* レコードを選択してTSV形式の文字列としてコピーする

## 今後追加する機能
* 接続プロファイルを作成、編集するUIをページ形式に変更する
* 接続プロファイルを複製できるようにする
* Table、Viewの詳細画面として「制約」、「インデックス」タブを追加する
* テーブルデータの一括エクスポート機能を追加する
  - CSV/TSV/InsertSQL形式でのエクスポートを想定
* TSV形式のレコードをペーストしてInsertできるようにする
* Java開発向けの便利機能
  - UI上からEntityやRecordを生成（Table、View対象）
* 「データ」タブの機能拡充
  - データリロード
  - ヘッダ項目クリックによるデータソート
  - 簡易データ検索機能（where句を指定するようなイメージ）
  - 行Update（コミット、ロールバック対応）

## 推奨ロードマップ
* v2.1
  - [実装済み] 接続プロファイルの作成、編集UIをWebviewのページ形式に変更する
  - [実装済み] 接続プロファイルの複製機能を追加する
  - [実装済み] 接続テスト結果やJDBCドライバー/サポートJAR選択を分かりやすく表示する
* v2.1.x
  - [一部実装済み] UX改善としてツリー情報のリフレッシュ導線を強化する
    - [実装済み] ビュー右上からRefreshできるようにする
    - [実装済み] 接続プロファイルの追加、編集、削除、複製後は自動Refreshする
    - 接続ノード右クリックからRefreshできるようにする
    - Tables/Viewsフォルダ単位でRefreshできるようにする
    - [実装済み] READMEに、DB側でTable/Viewを追加・削除した場合はRefreshする旨を記載する
* v2.2
  - Table、View詳細に「制約」タブを追加する
  - Table、View詳細に「インデックス」タブを追加する
  - 主キー、外部キー、Unique、Indexなどのメタデータ取得を強化する
  - InsertSQLコピーでOracleの日付/時刻型に対応する
    - JDBC型情報を取得し、DATE/TIMESTAMP/TIMESTAMP WITH TIME ZONEなどを判別する
    - Oracle向けにDATEリテラル、TIMESTAMPリテラル、必要に応じてTO_DATE/TO_TIMESTAMP形式を生成する
    - PostgreSQL/MySQL/H2は現状の標準寄りリテラルを維持しつつ、型情報を使った改善を検討する
* v2.3
  - テーブルデータの一括エクスポート機能を追加する
  - CSV/TSV/InsertSQL形式での保存に対応する
  - 大量データ向けに件数制限、進捗表示、キャンセルを検討する
* v3.0
  - TSV形式のレコードをペーストしてInsertできるようにする
  - 書き込み前の確認、トランザクション、失敗時の扱いを設計する
* v3.1以降
  - Java Entity/Record生成機能を追加する
  - 型マッピング、命名規則、package、JPA/Jakarta対応などを設定可能にする
